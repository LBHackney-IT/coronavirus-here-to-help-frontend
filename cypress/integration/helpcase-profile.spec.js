describe('View helpcase profile page', () => {
    // it('Resident key information is displayed', () => {
    //     cy.visit(`http://localhost:3000/helpcase-profile/1`);
    //     cy.get('[data-testid=resident-name_header]').should('contain', "Firstname Lastname");
    //     cy.get('[data-testid=key-information_phone-number_0]').should('contain', "00000000000");
    //     cy.get('[data-testid=key-information_phone-number_2]').should('contain', "22222222222");
    //     cy.get('[data-testid=key-information_resident-address]').should('contain', "Some place");
    //     cy.get('[data-testid=key-information_resident-address]').should('contain', "Some street");
    //     cy.get('[data-testid=key-information_resident-address]').should('contain', "Some house");
    //     cy.get('[data-testid=key-information_resident-address]').should('contain', "A1 2BC");
    // });
    // Commented out because these tests will be covered in the unit tests in the future
});