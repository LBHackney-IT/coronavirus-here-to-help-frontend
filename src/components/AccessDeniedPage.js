import Layout from './layout';

export const AccessDeniedPage = () => {
    return (
        <Layout>
            <h1> You don't have the required permissions to use this service. Please contact ICT if you think you should have access to it. </h1>
        </Layout>
    );
};
